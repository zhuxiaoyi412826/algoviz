# 什么是GithupFlow

> 一个轻量级分支的持续交付工作流：为每次变更创建一个分支，打开拉取请求（PR），运行CI/审查，合并到主节点，然后部署

逐步工作流程

1. 为你的工作创建一个分支

   - 使用简短且具描述性的名称（特征/、修复/、任务/）。

   - 示例：

     ```
     git checkout -b feature/add-login
     ```

2. 做一些小而有针对性的承诺

   - 保持变化小而渐进;多用力推。

     ```
     git add .
     git commit -m "Add basic login form"
     git push -u origin feature/add-login
     ```

3. 开启拉取请求

   - 请描述你更改了哪些内容、原因以及任何测试步骤。
   - 如果有帮助，请附上相关问题的链接，并附上截图或测试结果。

4. 持续集成与代码审查

   - CI应该运行自动化测试、线条处理和构建步骤。
   - 至少获得一个批准审核，用新的提交回复评论。

5. 合并到主服务器（一旦批准并绿色）

   - 按照团队惯例使用压缩/合并或合并提交。
   - 我更倾向于通过GitHub界面合并来触发受保护分支规则。

6. 从主部署

   - 合并后立即部署或自动部署（取决于你的流水线）。

7. 删除该分支

   - 合并后清理：

     ```
     git push origin --delete feature/add-login
     ```

最佳实践

- 保持分支寿命短暂（数小时至数天，而非数月）。
- 让PR小而聚焦，以便更容易复核。
- 要求CI在合并前至少有一次批准审核。
- 保护主武器：要求进行状态检定、分支保护和限制推力。
- 使用有意义的公关标题和模板，要求测试步骤和影响力。
- 自动化从主发布（持续部署）或基于标签的发布中的部署。
- 如果你的团队同意，建议用重基来做线性历史;否则就用合并提交。