<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.algoviz.mapper.OJSolutionLikeMapper">

    <resultMap id="LikeMap" type="com.algoviz.entity.OJSolutionLike">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectByUserAndTarget" resultMap="LikeMap">
        SELECT id, user_id, target_type, target_id, created_at
        FROM oj_solution_like
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO oj_solution_like (user_id, target_type, target_id)
        VALUES (#{userId}, #{targetType}, #{targetId})
    </insert>

    <delete id="deleteByUserAndTarget">
        DELETE FROM oj_solution_like
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId}
    </delete>

</mapper>
