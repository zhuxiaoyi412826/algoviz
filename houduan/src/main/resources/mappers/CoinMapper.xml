<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.algoviz.mapper.CoinMapper">

    <resultMap id="CoinProductMap" type="com.algoviz.entity.CoinProduct">
        <id     column="id"           property="id"/>
        <result column="product_id"   property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="description"  property="description"/>
        <result column="coin_price"   property="coinPrice"/>
        <result column="category"     property="category"/>
        <result column="icon"         property="icon"/>
        <result column="status"       property="status"/>
        <result column="created_at"   property="createdAt"/>
        <result column="updated_at"   property="updatedAt"/>
    </resultMap>

    <resultMap id="CoinPurchaseMap" type="com.algoviz.entity.CoinPurchase">
        <id     column="id"           property="id"/>
        <result column="user_id"      property="userId"/>
        <result column="username"     property="username"/>
        <result column="product_id"   property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="coin_price"   property="coinPrice"/>
        <result column="coin_before"  property="coinBefore"/>
        <result column="coin_after"   property="coinAfter"/>
        <result column="status"       property="status"/>
        <result column="created_at"   property="createdAt"/>
    </resultMap>

    <!-- ===== 商品查询 ===== -->

    <select id="getAllActiveProducts" resultMap="CoinProductMap">
        SELECT * FROM coin_product WHERE status = 'ACTIVE' ORDER BY coin_price ASC
    </select>

    <select id="getAllProducts" resultMap="CoinProductMap">
        SELECT * FROM coin_product ORDER BY created_at DESC
    </select>

    <select id="getProductById" resultMap="CoinProductMap">
        SELECT * FROM coin_product WHERE product_id = #{productId}
    </select>

    <insert id="insertProduct" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO coin_product (product_id, product_name, description, coin_price, category, icon, status)
        VALUES (#{productId}, #{productName}, #{description}, #{coinPrice}, #{category}, #{icon}, #{status})
    </insert>

    <!-- 动态更新：仅更新入参中非 null 的字段，避免 Knife4j/REST 表单漏传字段把名称/价格/图标等覆盖为 null -->
    <update id="updateProduct">
        UPDATE coin_product
        <set>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="description != null">description  = #{description},</if>
            <if test="coinPrice != null">coin_price   = #{coinPrice},</if>
            <if test="category != null">category     = #{category},</if>
            <if test="icon != null">icon         = #{icon},</if>
            <if test="status != null">status       = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE product_id = #{productId}
    </update>

    <delete id="deleteProduct">
        DELETE FROM coin_product WHERE product_id = #{productId}
    </delete>

    <!-- ===== 购买记录 ===== -->

    <insert id="insertPurchase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO coin_purchase (user_id, username, product_id, product_name, coin_price, coin_before, coin_after, status)
        VALUES (#{userId}, #{username}, #{productId}, #{productName}, #{coinPrice}, #{coinBefore}, #{coinAfter}, #{status})
    </insert>

    <select id="getPurchasesByUserId" resultMap="CoinPurchaseMap">
        SELECT * FROM coin_purchase WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="getAllPurchases" resultMap="CoinPurchaseMap">
        SELECT * FROM coin_purchase ORDER BY created_at DESC
    </select>

    <select id="getPurchasesByPage" resultMap="CoinPurchaseMap">
        SELECT * FROM coin_purchase
        <where>
            <if test="keyword != null and keyword != ''">
                username LIKE CONCAT('%', #{keyword}, '%')
                OR product_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPurchasesForPage" resultType="int">
        SELECT COUNT(*) FROM coin_purchase
        <where>
            <if test="keyword != null and keyword != ''">
                username LIKE CONCAT('%', #{keyword}, '%')
                OR product_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="sumUserSpent" resultType="int">
        SELECT COALESCE(SUM(coin_price), 0) FROM coin_purchase WHERE user_id = #{userId} AND status = 'SUCCESS'
    </select>

    <select id="sumAllSpent" resultType="int">
        SELECT COALESCE(SUM(coin_price), 0) FROM coin_purchase WHERE status = 'SUCCESS'
    </select>

    <select id="searchCoinProducts" resultMap="CoinProductMap">
        SELECT * FROM coin_product
        <where>
            <if test="keyword != null and keyword != ''">
                product_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="startDate != null and startDate != ''">
                AND created_at &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countSearchCoinProducts" resultType="long">
        SELECT COUNT(*) FROM coin_product
        <where>
            <if test="keyword != null and keyword != ''">
                product_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="startDate != null and startDate != ''">
                AND created_at &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            </if>
        </where>
    </select>

    <select id="countAllPurchases" resultType="int">
        SELECT COUNT(*) FROM coin_purchase WHERE status = 'SUCCESS'
    </select>

    <!-- ===== 删除引用保护 ===== -->

    <!-- 指定分类下金币商品数（删除分类前检查） -->
    <select id="countCoinProductsByCategory" resultType="long">
        SELECT COUNT(*) FROM coin_product WHERE category = #{category}
    </select>

    <!-- 指定金币商品的购买记录数（删除金币商品前检查） -->
    <select id="countPurchasesByProductId" resultType="long">
        SELECT COUNT(*) FROM coin_purchase WHERE product_id = #{productId}
    </select>

</mapper>
