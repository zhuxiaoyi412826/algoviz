<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.algoviz.mapper.FeedbackMapper">
    
    <resultMap id="FeedbackResultMap" type="com.algoviz.entity.Feedback">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="email" property="email" />
        <result column="type" property="type" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="status" property="status" />
        <result column="reply" property="reply" />
        <result column="reply_time" property="replyTime" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <select id="findByPage" resultMap="FeedbackResultMap">
        SELECT * FROM feedback ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM feedback
    </select>
    
    <select id="getAllFeedbacks" resultMap="FeedbackResultMap">
        SELECT * FROM feedback ORDER BY create_time DESC
    </select>
    
    <select id="getFeedbacksByUserId" resultMap="FeedbackResultMap">
        SELECT * FROM feedback WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="getFeedbacksByStatus" resultMap="FeedbackResultMap">
        SELECT * FROM feedback WHERE status = #{status} ORDER BY create_time DESC
    </select>
    
    <select id="getFeedbacksByType" resultMap="FeedbackResultMap">
        SELECT * FROM feedback WHERE type = #{type} ORDER BY create_time DESC
    </select>
    
    <select id="findById" resultMap="FeedbackResultMap">
        SELECT * FROM feedback WHERE id = #{id}
    </select>
    
    <insert id="insert">
        INSERT INTO feedback (user_id, username, email, type, title, content, status, create_time, update_time)
        VALUES (#{userId}, #{username}, #{email}, #{type}, #{title}, #{content}, #{status}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update">
        UPDATE feedback SET 
            type = #{type},
            title = #{title},
            content = #{content},
            status = #{status},
            reply = #{reply},
            reply_time = #{replyTime},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
    
    <update id="updateFeedbackReply">
        UPDATE feedback SET 
            reply = #{reply},
            status = #{status},
            reply_time = CURRENT_TIMESTAMP,
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
        <!-- email -->
    <delete id="deleteById">
        DELETE FROM feedback WHERE id = #{id}
    </delete>
    
    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM feedback WHERE status = #{status}
    </select>
</mapper>